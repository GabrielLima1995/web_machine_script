#!/bin/bash
sudo apt-get install pandoc 
sudo apt-get install python3-sphinx
sudo -H pip install sphinx-autobuild
sudo -H pip install jupyter-sphinx
sudo -H pip install nbsphinx
sudo -H pip install sphinx-rtd-theme
sudo -H pip install pip install sphinx-rtd-dark-mode
sudo -H pip install sphinx-math-dollar

